<h1>INBOX</h1>
<ul>
  <% @conversations.each do |conversation| %>
  <li><%= link_to conversation.recipient.visible_name, inbox_messages_show_path(conversation)  %></li>
  <% end %>
</ul>

<% if @conversation %>
  <%= render partial: 'inbox_show' %>
<% end %>
