<h4 class= "unfavorite-button">
<%= project.favorites.length %>
<%= link_to image_tag("heart-red.png"), favorite_path(Favorite.find_by(project_id: project.id, user_id: current_user.id)), method: :delete, remote: true %>
</h4>