<h4 class= "favorite-button">
  <%= project.favorites.length %>
  <%= link_to image_tag("heart-grey.png", class: "heart"), favorites_path(project_id: project.id, user_id: current_user.id), method: :post, remote: true %>
</h4>
