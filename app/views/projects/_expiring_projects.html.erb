<div id="expiring" class= "sorted-projects">

    <h1 class = "project-category-1">PROJECTS EXPIRING SOON</h1>

  <% expiring_soon.each do |project| %>
    <%= render partial: "index_show", locals: { project: project } %>
    <div class="divider">
         </div>
   <% end %>
</div>
