<span id="unreport-button">
  <%=link_to 'Reported, Click to Remove Flag', flagged_project_path(FlaggedProject.find_by(project_id: project.id, user_id: current_user.id)), method: :delete, remote: true %>
</span>